/target/
*.duckdb
*.duckdb.wal
models/*.onnx
models/*.onnx.data
# basis LUTs, produced alongside lut3d_predictor.onnx
models/*.npy
# upstream checkpoints cached by tools/export_lut3d.py — FiveK-derived weights,
# research-use only, never redistributed from this repo
tools/.checkpoints/
# subagent-driven-development scratch
.superpowers/

# python bytecode cache (tools/ export scripts)
__pycache__/

# editor / IDE
.idea/
.claude/

# playwright MCP browser-test artifacts + screenshots
.playwright-mcp/
/*.png

# local planning scratch (not part of the repo)
PHASE_*_PROMPT.md
PROJECT_OVERVIEW.md

# stray copy-export trees accidentally created in the repo root
/_keepers/

# Local sample RAWs for develop testing — large binaries, not repo content.
example-pictures/
.DS_Store
